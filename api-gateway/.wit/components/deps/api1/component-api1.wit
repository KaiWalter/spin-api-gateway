package component:api1;

interface data-handler {
  record my-object {
    name: string,
    steps: u32,
    processed: option<bool>,
  }

  handle-data: func(key: my-object) -> my-object;
}

package root:component {
  world root {
    export component:api1/data-handler;
  }
}
